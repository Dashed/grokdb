const React = require('react');

const StashCards = React.createClass({

    render() {
        return (
            <div>
                {'stash cards'}
            </div>
        );
    }
});

module.exports = StashCards;
