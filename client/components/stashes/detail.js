const React = require('react');

const StashDetail = React.createClass({

    render() {
        return (
            <div>
                {'stash detail'}
            </div>
        );
    }
});

module.exports = StashDetail;
