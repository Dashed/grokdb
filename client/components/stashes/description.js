const React = require('react');

const StashDescription = React.createClass({

    render() {
        return (
            <div>
                {'stash description'}
            </div>
        );
    }
});

module.exports = StashDescription;
