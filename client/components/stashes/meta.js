const React = require('react');

const StashMeta = React.createClass({

    render() {
        return (
            <div>
                {'stash meta'}
            </div>
        );
    }
});

module.exports = StashMeta;
