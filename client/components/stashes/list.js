const React = require('react');

const courier = require('courier');

const StashesList = React.createClass({

    render() {
        return (
            <div>
                {'list'}
            </div>
        );
    }
});

module.exports = StashesList;
